import React, { SFC } from 'react';

type LogInFormProps = {};

const LogInForm: SFC<LogInFormProps> = (props): JSX.Element => {
  return (
    <form>
      lodihaslihjdas dposalkadjs ;asdjaks akopsdjadklsahl opsadjapsjda pas dajsd
    </form>
  );
};

export default LogInForm;
